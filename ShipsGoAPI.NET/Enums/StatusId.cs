namespace ShipsGoAPI.NET.Enums
{
    public enum StatusId
    {
        InProgress = 20,
        Booked = 30,
        Loaded = 35,
        Sailing = 40,
        Arried = 45,
        Discharged = 50,
        Untracked = 60
    }
}
